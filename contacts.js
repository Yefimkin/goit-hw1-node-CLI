const fs = require("fs");

const contactsPath = async () => {};

const filepath = "./db/contacts.json";
contactsPath({ filepath });

// const contactsPath = require("./db/contacts.json");
// console.log(contactsPath);
